# F3 — S-1030 / S-1040 / S-1060 Promotion

> **Round-6 Batch F3.** XML/event owner. Hard blocker from R5. Parallel.

## Read first

- [`../plan.md`](../plan.md) — Carryover Backlog Batch F3.
- R5 prompt `D2-s1030-s1040-s1060-promotion.md` (the original
  attempt; see why it didn't close).
- `docs/work/round-1/leiaute-blockers.md` if present.
- Round-1 prompt `01-remaining-tables.md` for builder conventions.

## Decision (locked)

**D3 = (b) — Bind to current leiaute** for all three families,
including S-1060.

For S-1060: fetch the current `evtTabAmbiente.xsd` from gov.br
publication; **regenerate** `s1060.golden.xml` against the new
schema; promote the builder normally. The legacy `v02_05_00` golden
is replaced, not retained as evidence — the round-1 leiaute-blocker
note in `docs/work/round-1/leiaute-blockers.md` resolves to the new
version.

File ADR `docs/adrs/<next>-s1060-leiaute-decision.md` documenting:
- Old version: `evtTabAmbiente/v02_05_00`.
- New version: current gov.br publication.
- Reason: S-1060 remains an active eSocial event class as of
  round-6 entry.

**Risk note** (per plan risk register): if gov.br has not published a
current `evtTabAmbiente.xsd` when F3 runs, escalate at start. Route
the external S-1060 source dependency to Round 7 with a separate ADR;
promote S-1030 + S-1040 normally
and update the closure target — F3 closes 2/3 with S-1060 named in
the Round 7 deferred-external plan.

## Tasks

For **each** of S-1030, S-1040, S-1060 (D3=(b) means uniform treatment):

1. **Fetch current XSD from gov.br publication.**
   - S-1030: `evtTabCargo.xsd` → `packages/domain/src/xml/xsd/s1030/`.
   - S-1040: `evtTabFuncao.xsd` → `packages/domain/src/xml/xsd/s1040/`.
   - S-1060: current `evtTabAmbiente.xsd` →
     `packages/domain/src/xml/xsd/s1060/`. Regenerate
     `docs/templates/golden/builders/s1060.golden.xml` against the
     new schema as part of step 7.
2. **DTO**: replace `Round1Pending` with concrete shape at
   `packages/contracts/src/dtos/<family>.ts`.
3. **JSON Schema** generated under `packages/contracts/schemas/v1/`.
4. **Example payload** under
   `packages/contracts/examples/v1/requests/`.
5. **Active builder** at
   `packages/domain/src/builders/<family>/builder.ts`.
   - Pure function `build(dto, ctx) → { xml, metadata }`.
   - Reuses helpers from S-1000 / S-1010 / S-1020 / S-1050 / S-1070.
   - No `sgp-lifted/` imports.
6. **Builder export** + dispatcher entry in
   `packages/domain/src/submission/submission-dispatcher.ts`.
7. **Golden test** at `tests/golden/<family>.test.ts` — byte-equal
   against the committed golden XML.
8. **Metadata test** asserting `eventCode`, `leiauteVersion`,
   `xmlRoot`, `xsdBinding`, dependencies match `docs/events.md`.
9. **Invalid-DTO test** with typed `DtoValidationError` and field
   paths.
10. **Integration suite inclusion** in
    `tests/integration/soap-submission-pipeline.test.ts`.
11. **`Round1Pending` cleanup**: remove the family from
    `packages/contracts/src/dtos/round1-pending.ts`. After F3,
    `Round1Pending` should be empty.
12. **`docs/events.md`, `docs/consumers.md`, `docs/sgp-migration.md`**
    updated with the per-family DTO + metadata.
13. **ADRs filed** for any retire-vs-bind decision (S-1060 under D3).

## Primary write scope

- `packages/contracts/src/dtos/{s1030,s1040,s1060}.ts`
- `packages/contracts/src/dtos/round1-pending.ts` (remove three)
- `packages/contracts/schemas/v1/{s1030,s1040,s1060}.json`
- `packages/contracts/examples/v1/requests/{s1030,s1040,s1060}.json`
- `packages/domain/src/xml/xsd/{s1030,s1040,s1060}/`
- `packages/domain/src/builders/{s1030,s1040,s1060}/`
- `packages/domain/src/builders/index.ts`
- `packages/domain/src/submission/submission-dispatcher.ts`
- `tests/golden/{s1030,s1040,s1060}.test.ts`
- `tests/golden/fixtures/{s1030,s1040,s1060}.dto.json`
- `tests/integration/soap-submission-pipeline.test.ts`
- `docs/events.md`, `docs/consumers.md`, `docs/sgp-migration.md`
- `docs/adrs/<next>-s1060-leiaute-decision.md`
- `docs/release/1.2.0/families/`

## Do not touch

- Other active builders.
- Other carry-over batches.
- R6 expansion batches (A–E).

## Exit criteria

- All three families ACTIVE_FULL (D3=(b) bind-to-current uniformly).
- 35 / 35 non-return classes ACTIVE_FULL.
- `Round1Pending` empty / removed.
- All gates green.
- ADR filed for D3.

## Verification

```text
npm run build
npm run lint
npm run test:integration
ls packages/domain/src/builders | grep -E "^s10(30|40|60)"
node -e "const c=require('./packages/contracts/dist'); console.log(c.ESOCIAL_EVENT_CLASSES.length);"
# expect: 39
```

Report: XSD source per family, S-1060 D3 choice, ADR filed, integration
case count, classes ACTIVE_FULL delta.
