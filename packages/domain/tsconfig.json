{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/tsconfig.tsbuildinfo"
  },
  "references": [
    { "path": "../contracts" },
    { "path": "../pki-pades" }
  ],
  "include": ["src/**/*.ts"],
  // Round 0 fences SGP lift-out evidence from active builds; Round 1 promotes remaining slices.
  "exclude": ["src/sgp-lifted/**", "dist", "node_modules"]
}
